// Ejemplo de función para integrar MCP o Google Vision
export async function identifyLetter(imageBase64) {
  // Aquí implementas llamada a MCP / Google OCR
  // Retorna la letra detectada
  // Por ahora mock:
  return "A";
}